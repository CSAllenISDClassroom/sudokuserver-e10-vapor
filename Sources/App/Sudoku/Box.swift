import Foundation

public class Box : NineCells {
    
    // Class Box inherits the properties from parent class NineCells       
    
    /*init(boxIndex:Int) {
        var cells = [Cell]()
        for cellIndex in 0 ... 8 {
            cells.append(Cell(value:cellIndex, boxIndex: boxIndex, cellIndex: cellIndex))
            //(position: Position(boxIndex: boxIndex, cellIndex: cellIndex), value: cellIndex))            
        }        
    }
    
     */
}
